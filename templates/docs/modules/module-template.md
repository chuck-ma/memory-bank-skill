# {Module} 模块

> 更新于: {YYYY-MM-DD}

## 职责
{模块负责什么，1-2 句话}

## 目录结构
```
src/{path}/
├── components/      # {说明}
├── hooks/           # {说明}
├── services/        # {说明}
└── types.ts         # {说明}
```

## 核心流程
```
{输入} → {处理1} → {处理2} → {输出}
```

## 对外接口
| 接口 | 说明 | 详见 |
|------|------|------|
| {接口名} | {说明} | [specs/{spec}.md](../specs/{spec}.md) |

## 依赖关系
- **依赖**: {本模块依赖的其他模块}
- **被依赖**: {依赖本模块的其他模块}

## 变更记录
| 日期 | 变更 | 原因 |
|------|------|------|
| {YYYY-MM-DD} | {变更内容} | {为什么改} |
